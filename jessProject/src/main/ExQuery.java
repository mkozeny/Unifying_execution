package main;

import jess.*;

public class ExQuery {
	/*public static void main(String[] argv) throws JessException {
		Rete engine = new Rete();
		engine.batch("main/hello.clp");
		engine.reset();
		QueryResult result = engine.runQueryStar("search-by-name",
				new ValueVector().add("Smith"));
		while (result.next()) {
			System.out.println(result.getString("fn") + " "
					+ result.getString("ln") + ", age" + result.getInt("age"));
		}
		System.out.println("Number of query results: "+engine.countQueryResults("search-by-name",
				new ValueVector().add("Smith")));
	}*/
}
